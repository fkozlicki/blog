import styled from 'styled-components';

export const Header = styled.header`
  height: 400px;
  background: #dce2ef;
  position: relative;

  .wave {
    position: absolute;
    bottom: 0;
    width: 100%;
  }
`;
