import styled from 'styled-components';

export const Header = styled.header`
  height: 400px;
  background: #dce2ef;
  position: relative;
  z-index: 0;

  .wave {
    position: absolute;
    bottom: 0;
    width: 100%;
  }
`;
